{% if include.id %}
  {% assign authors_row = page[include.id] %}
{% endif %}


<div class="authors__wrapper{% if include.type %}--{{ include.type }}{% endif %}">
  {% for author in authors_row %}
      {% capture page.author %} {{ author }} {% endcapture %}
      {% include author-profile.html %}
  {% endfor %}
</div>
